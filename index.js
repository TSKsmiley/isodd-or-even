//THIS IS NOT A JOKE!!! USE LOL
var none = require('none'); //this makes the program load faster!
var throwErr = require('is-number'); // we are using var instead of const for speed!
var isNum = require('throw-error')
var True = require('false');
var Oof = require('If'); //named oof so that it doesnt get confused with regular js if
var Camelcase = require('camelcase');
var sleep = require('system-sleep');
var log = require("log");//just in case we need it later
var chalk = require('chalk');
var spaceBar = "-"


module.exports = function(num) {
    console.log(chalk.bgHex("#ff00FF").bold(chalk.blue(Camelcase("-Chalkulating-the-answer" + spaceBar + `${num.toString(num)}`))))
        /* 5 seconds */sleep(1000);
    Oof(!throwErr(num)).Then(()=>{isNum(new Error(Camelcase("Given-parameter-was-not-a-number!")))})
return !True() ? !True() : True(); //here we(I) use a ternary operator to make sure that our result is always correct
isNum(new Error(Camelcase("HOW DID WE GET HERE?? (Minecraft)")));}//we camel case, just in case. Get it? ;)